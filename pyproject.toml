[project]
name = "python-template"
version = "0.1.0"
description = ""
authors = [{name = "Avito Monitor maintainers"}]
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.29,<4",
    "flask>=3.1.1",
    "requests>=2.32.4",
    "aiohttp>=3.9.5",
    "beautifulsoup4>=4.12.3",
    "python-dotenv>=1.0.1",
    "curl-cffi>=0.13.0",
]

[tool.setuptools]
py-modules = ["avito_monitor_bot", "alert_bot", "background", "storage"]
